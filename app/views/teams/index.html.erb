<h1>Index of Teams</h1>

<table class="table table-striped">
	<thead>
		<tr>
			<th>Team</th>
			<th>Location</th>
			<th>League</th>
		</tr>
	</thead>
	<tbody>
		<% @teams.each do |team| %>
			<tr>
				<td><%= link_to "#{team.school} #{team.nickname}", team_path(team) %> | <%= link_to 'Edit', edit_team_path(team) %> | <%= link_to 'Delete', team_path(team), method: :delete,data: { confirm: 'Are you sure?' } %></td>
				<td><%= team.city %>, <%= team.state %> <%= team.zip %></td>
				<td><%= team.league %></td>
			</tr>
		<% end %>
	</tbody>
</table>